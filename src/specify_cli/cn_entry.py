"""Deprecated stub kept for backwards compatibility.

The real Simplified Chinese entry point now lives in ``specify_cli_cn_entry``.
This module intentionally raises an ImportError if imported directly so the
new entry point is used instead.
"""

from __future__ import annotations


raise ImportError(
    "specify_cli.cn_entry is deprecated; use specify_cli_cn_entry instead."
)
